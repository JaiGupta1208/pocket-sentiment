# Copy to secrets.toml for local runs, or paste into Streamlit Cloud's Secrets manager.
# Never commit the real secrets.toml (it's in .gitignore).
ANTHROPIC_API_KEY = "sk-ant-api03-..."
YOUTUBE_API_KEY = "AIza..."
